using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using System.IO;
using AtworkAPI.Models;

namespace AtworkAPI.Helpers
{
    public class ImagesHelper
    {
        public void SaveImage(Object imageFile)
        {

        }

        //public List<Image> ReadImagesToModel(string searchString, string hostingPath)
        //{
        //    //DirectoryInfo dirInfo = new DirectoryInfo(System.Web.Hosting.HostingEnvironment.MapPath(""));
        //    FileInfo[] files = new DirectoryInfo(System.Web.Hosting.HostingEnvironment.MapPath(hostingPath))
        //                    .GetFiles(searchString);
        //    FileStream fs = files.
        //}
    }
}